TODO

* e.g., append .bashrc.local to the repo's .bashrc to generate .bashrc
* get global .gitignore working correctly
* add an init script
** git config --global core excludes ~/.gitignore etc
** git config --global name/email
* s/w to consider auto-installing
** git
** emacs
** pip
** setuptools
